# dependencies
node_modules/

# build output
dist/
out/
release/

# TypeScript incremental build cache
*.tsbuildinfo

# logs
*.log

# cli protocol probe artifacts (regenerate via npm run protocol)
smoke/*.jsonl
smoke/*.raw

# session/harness artifacts (ไม่ใช่โค้ดโปรเจกต์)
.claude/
.remember/
plans/

# dev-only: unit tests + process docs aren't shipped with the app
tests/
docs/superpowers/

# scratch / dev artifacts
tmp_logo/
.shot-demo/
shot*.png
tmp-*.mjs

# os
.DS_Store
Thumbs.db

# superpowers sdd scratch
.superpowers/
